[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "myapp"
version = "0.1.0"
description = "Example CLI calculator application"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
myapp = "myapp.cli:main"
